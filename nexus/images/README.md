# Images

Nexus page images, tracked in the repo.

- `thumbnail.jpg` - the main page image, and the "after" shot: the ammo tab with every stack at 0 weight.
- `01-before-vanilla-weight.jpg` - the "before" shot: the same inventory with the mod disabled, ammo at its real weight (total 210/165 encumbrance).

The two form a before/after pair on the same stacks, so the effect is obvious.
